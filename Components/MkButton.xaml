<UserControl x:Class="mkAutoClicker.Components.MkButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             x:Name="Root"
             Focusable="False"
             mc:Ignorable="d"
             d:Text="Button"
             d:Width="120"
             d:Height="30">
    <Grid>
        <Button x:Name="InnerButton"
                Focusable="True"
                FocusVisualStyle="{x:Null}"
                MinHeight="28"
                Background="{Binding ButtonBackground, ElementName=Root}"
                BorderBrush="{Binding ButtonBorderBrush, ElementName=Root}"
                Foreground="{Binding ButtonForeground, ElementName=Root}"
                BorderThickness="1"
                Padding="10,4"
                Cursor="Hand"
                d:Content="Button">
            <Grid HorizontalAlignment="Center"
                  VerticalAlignment="Center">
                <Path x:Name="IconPathElement"
                      Width="{Binding IconSize, ElementName=Root}"
                      Height="{Binding IconSize, ElementName=Root}"
                      Data="{Binding IconPathData, ElementName=Root}"
                      Stroke="{Binding ButtonForeground, ElementName=Root}"
                      StrokeThickness="{Binding IconStrokeThickness, ElementName=Root}"
                      Stretch="Uniform"
                      StrokeLineJoin="Round"
                      StrokeStartLineCap="Round"
                      StrokeEndLineCap="Round"
                      Visibility="Collapsed" />
                <TextBlock x:Name="TextElement"
                           Text="{Binding Text, ElementName=Root, TargetNullValue=Button, FallbackValue=Button}"
                           Foreground="{Binding ButtonForeground, ElementName=Root}" />
            </Grid>
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Border x:Name="FocusRing"
                                Margin="-1"
                                CornerRadius="7"
                                BorderThickness="2"
                                BorderBrush="{StaticResource Brush.FocusRing}"
                                Opacity="0" />
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="2" />
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.92" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.84" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="FocusRing" Property="Opacity" Value="0.35" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.5" />
                            <Setter TargetName="FocusRing" Property="Opacity" Value="0" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>
</UserControl>
